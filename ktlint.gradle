apply plugin: 'org.jlleitschuh.gradle.ktlint'

ktlint {
    verbose.set(true)
    android.set(true)
    outputToConsole.set(true)
    filter {
        exclude("**/generated/**")
        include("**/kotlin/**")
    }
}